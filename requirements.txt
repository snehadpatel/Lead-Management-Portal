# Core ML + API + dashboard
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
streamlit>=1.32
openpyxl>=3.1
pillow>=10.0
plotly>=5.18

# Big data (install matching PySpark for your Python version)
pyspark>=3.5.0

# Optional: gradient boosting (free); training falls back to RandomForest if missing
xgboost>=2.0.0

# Deep learning / LSTM scripts (optional)
# torch>=2.0
